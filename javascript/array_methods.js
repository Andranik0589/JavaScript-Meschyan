/* let x =[5, 10, 22];

debugger;
x.shift()
console.log(x)

console.log(x.shift()); */

//split

/* function spl(name) {
    let names = name.split(" ");
    let upperNames = names.map(function(val) {
        return val.substring(0, 1).toUpperCase() + val.substring(1, val.length).toLowerCase();
    });
    return upperNames.join(" ");
}
alert(spl("ando jan dzec")); */

//replaceAll

/* a = "who is the bomm";
let b = a.replaceAll("bomm","teacher");
alert(b[0]).; */
/* for(let i = 0; i <11;i++) {
alert(Math.floor( Math.random() *10));
}
 */



